<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/top_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:focusableInTouchMode="true"
    android:focusable="true"
    android:nextFocusDown="@id/tab_layout_1"
    tools:context=".MasterActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include layout="@layout/toolbar_general" />

        <include layout="@layout/tab_layout_1_degree"/>

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/lists_results_view_pager"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    </LinearLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        style="@style/fab_base_style"
        android:contentDescription="@string/content_description_discover_button"
        android:id="@+id/search_list_fab"
        android:src="@drawable/ic_discover"/>

</FrameLayout>