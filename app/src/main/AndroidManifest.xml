<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.atmko.onmywatch">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name="com.atmko.onmywatch.widget.ListWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/list_widget_provider_info" />
        </receiver>

        <receiver android:name=".utils.NotificationHandler$BootReceiver"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <receiver android:name=".utils.NotificationHandler$AlarmReceiver"/>

        <receiver android:name=".utils.NotificationHandler$TimeChangedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET"/>
            </intent-filter>
        </receiver>

        <service android:name=".utils.UpdateNotifierService" android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <service android:name=".utils.SeriesTracker" android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <activity
            android:name=".MasterActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.atmko.onmywatch.widget.ListWidgetProviderConfigureActivity"
            android:launchMode="singleInstance"
            android:taskAffinity=".widget_setup"
            android:autoRemoveFromRecents="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity android:name=".CreateListActivity"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.PopupDialog"
            android:launchMode="singleTop"/>

        <activity android:name=".RateActivity"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.PopupDialog"
            android:launchMode="singleTop"/>

        <activity
            android:name=".AddToListActivity"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.PopupDialog"
            android:launchMode="singleTop"/>

        <activity
            android:name=".SettingsActivity"
            android:label="@string/options_settings_title"
            android:theme="@style/preferences_theme"
            android:launchMode="singleTop"/>

        <activity android:name=".BillingActivity"/>

        <service android:name=".utils.network_utils.ProModeMigrationService"
            android:exported="false"
            android:description="@string/pro_migration_service_description"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <service android:name=".utils.network_utils.FreeModeMigrationService"
            android:exported="false"
            android:description="@string/free_migration_service_description"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <service
            android:name=".widget.ListRemoteViewFactoryService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <service android:name=".widget.ListWidgetService" android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <!--ad mob application id key: currently in test mode-->
        <meta-data
            android:name="@string/ad_mob_application_id_key"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

    </application>

</manifest>